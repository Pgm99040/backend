  <!DOCTYPE html>
  <html>
    <head>
          <meta charset="utf-8">
          <meta name="viewport" content="width=device-width, initial-scale=1.0">
          <title>Reset your password</title>
          
          <link href='http://fonts.googleapis.com/css?family=Nunito:400,300' rel='stylesheet' type='text/css'>
          <link href="/stylesheets/main.css" rel="stylesheet" type="text/css">
        
          <script type="text/javascript" src="http://ajax.googleapis.com/ajax/libs/jquery/1.8.3/jquery.min.js"></script>
          <script type="text/javascript">

            $(function () {
                $("#btnSubmit").click(function () {
                  var password = $("#password").val();
                  var confirmPassword = $("#confirmPassword").val();

                  var invalid = " "; // Invalid character is a space
                    if (document.myForm.password.value.indexOf(invalid) > -1) {
                      alert("Sorry, spaces are not allowed.");
                      return false;
                    }
                    if (document.myForm.confirmPassword.value.indexOf(invalid) > -1) {
                      alert("Sorry, spaces are not allowed.");
                      return false;
                    }

                    if(!password) {
                      alert("Password field can not be empty.");
                      return false;
                    }
                    if(!confirmPassword) {
                      alert("Confirm password field can not be empty.");
                      return false;
                    }
                    if((!password)&&(!confirmPassword)) {s
                      alert("Password fields can not be empty.");
                      return false;
                    }
                    if(password != confirmPassword) {
                      alert("Passwords do not match.");
                      return false;
                    }
                    return true;
                });
            });
          </script>

      </head>
      <body>

        <div class="formClass">
        <form name=myForm  action="/resetPassword" method="post">
          <div class="logo_class"> 
            <p> 
              <a href="http://www.knowledgelocker.com/" target=_blank><img src="code-diy loge"/></a> 
            </p>
          </div>
          <h4>Reset your password</h4>
          
          <fieldset>
            
            <label for="password">Password:</label>
            <input type="password" id="password" name="password" minlength="6" maxlength="15" required>

            <label for="confirmation">Confirm password:</label>
            <input type="password" id="confirmPassword" name="confirmPassword" minlength="6" maxlength="15" required>

            <div>
              <input type="hidden" name="email" value="<%=email %>">
            </div>

         </fieldset>

          <button type="submit" id="btnSubmit">Submit</button>
        </form>
        </div>
      </body>
</html>
